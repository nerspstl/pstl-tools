import settings_plasma as plasma
import settings_probe as probe
import settings_solver as solver


settings = {
    "plasma"    :   plasma.XenonPlasma(),   # plasma.setup(**plasma.settings)
    "probe"     :   
}